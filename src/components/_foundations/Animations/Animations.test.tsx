import React from 'react'
import { render } from '@testing-library/react'
import Animations from './Animations'

describe('Animations Component', () => {
  test('Animations renders', () => {
    render(<Animations />)
  })
})
