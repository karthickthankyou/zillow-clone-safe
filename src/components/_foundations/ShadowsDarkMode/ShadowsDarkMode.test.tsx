import React from 'react'
import { render } from '@testing-library/react'
import ShadowsDarkMode from './ShadowsDarkMode'

describe('ShadowsDarkMode Component', () => {
  test('should render', () => {
    render(<ShadowsDarkMode />)
  })
})
