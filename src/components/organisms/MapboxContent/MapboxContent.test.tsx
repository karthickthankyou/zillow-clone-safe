import React from 'react'
import { render } from '@testing-library/react'
import MapboxContent from './MapboxContent'

describe('MapboxContent Component', () => {
  test('MapboxContent renders', () => {
    render(<MapboxContent />)
  })
})
