import React from 'react'
import { render } from '@testing-library/react'
import PropertyCard from './PropertyCard'

describe('PropertyCard Component', () => {
  test('PropertyCard renders', () => {
    render(<PropertyCard />)
  })
})
