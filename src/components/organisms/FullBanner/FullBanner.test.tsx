import React from 'react'
import { render } from '@testing-library/react'
import FullBanner from './FullBanner'

describe('FullBanner Component', () => {
  test('FullBanner renders', () => {
    render(<FullBanner />)
  })
})
