import React from 'react'
import { render } from '@testing-library/react'
import ChakraSlider from './ChakraSlider'

describe('ChakraSlider Component', () => {
  test('ChakraSlider renders', () => {
    render(<ChakraSlider />)
  })
})
