import React from 'react'
import { render } from '@testing-library/react'
import Streams from './Streams'

describe('Streams Component', () => {
  test('Streams renders', () => {
    render(<Streams />)
  })
})
