import React from 'react'
import { render } from '@testing-library/react'
import PopoverFullWidth from './PopoverFullWidth'

describe('PopoverFullWidth Component', () => {
  test('PopoverFullWidth renders', () => {
    render(<PopoverFullWidth />)
  })
})
