import React from 'react'
import { render } from '@testing-library/react'
import MapMarker from './MapMarker'

describe('MapMarker Component', () => {
  test('MapMarker renders', () => {
    render(<MapMarker />)
  })
})
