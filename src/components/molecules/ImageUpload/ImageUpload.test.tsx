import React from 'react'
import { render } from '@testing-library/react'
import ImageUpload from './ImageUpload'

describe('ImageUpload Component', () => {
  test('ImageUpload renders', () => {
    render(<ImageUpload />)
  })
})
