import React from 'react'
import { render } from '@testing-library/react'
import HighButton from './HighButton'

describe('HighButton Component', () => {
  test('HighButton renders', () => {
    render(<HighButton />)
  })
})
