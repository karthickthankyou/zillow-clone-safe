import React from 'react'
import { render } from '@testing-library/react'
import HtmlSelect from './HtmlSelect'

describe('HtmlSelect Component', () => {
  test('HtmlSelect renders', () => {
    render(<HtmlSelect />)
  })
})
